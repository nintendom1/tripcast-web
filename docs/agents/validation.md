# Frontend Validation

1. Run `npm run validate`.
2. Covers: `typecheck` → `lint` → `test`.
3. Do not claim completion until the command returns exit code 0.

`npm run dev` is available, but not for agents.