title = "TripCast"

[extend]
useDefault = true

[[rules]]
id = "tripcast-convex-url"
description = "Convex deployment or site URL committed to source"
regex = '''https?://[a-z0-9-]+\.convex\.(cloud|site)'''
tags = ["convex", "deployment-url"]
